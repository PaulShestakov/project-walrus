

const defaultState = {
	company: {}
};

const editCompanyReducer = (state = defaultState, action) => {
	switch (action.type) {


		default: {
			return state;
		}
	}
};

export default editCompanyReducer;